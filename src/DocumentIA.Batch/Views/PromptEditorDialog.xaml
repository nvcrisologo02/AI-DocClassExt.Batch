<Window x:Class="DocumentIA.Batch.Views.PromptEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editor de Prompts"
        Height="560"
        Width="760"
        MinHeight="500"
        MinWidth="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="TipologiaText" FontSize="18" FontWeight="SemiBold" />
            <TextBlock Text="Los cambios se guardan como override local para la tipología seleccionada."
                       Foreground="{StaticResource BrushMuted}"
                       Margin="0,4,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="System Prompt" Foreground="{StaticResource BrushMuted}" />
            <TextBox x:Name="SystemPromptTextBox"
                     Grid.Row="1"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="User Prompt Template" Foreground="{StaticResource BrushMuted}" />
            <TextBox x:Name="UserPromptTemplateTextBox"
                     Grid.Row="1"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </Grid>

        <DockPanel Grid.Row="3" LastChildFill="False">
            <Button Content="Restaurar por defecto" Click="RestoreDefault_Click" DockPanel.Dock="Left" />
            <Button Content="Guardar" Click="Save_Click" DockPanel.Dock="Right" Margin="8,0,0,0" />
            <Button Content="Cancelar" Click="Cancel_Click" DockPanel.Dock="Right" Background="#374151" />
        </DockPanel>
    </Grid>
</Window>
